import {
  type UseJournalTableLoadDataProps,
  useJournalTableLoadData
} from "src/utils/hooks/journals/useJournalTableLoadData";

export const JournalTableLoadData = (props: UseJournalTableLoadDataProps) => {
  useJournalTableLoadData(props);

  return <></>;
};
