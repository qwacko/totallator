<script lang="ts">
	import { browser } from '$app/environment';
	import { getSession } from 'lucia-sveltekit/client';

	const session = getSession();

	$: if ($session) {
		if (browser) {
			window.location.href = '/';
		}
	}
</script>

<div class="flex min-h-screen w-screen place-content-center bg-gradient-to-r from-indigo-300">
	<div class="m-10 mt-32  flex h-min rounded-md bg-white p-4 shadow-2xl ring-2 ">
		<slot />
	</div>
</div>
